export interface AppCreateBody {
  slug?: string;
  name?: string;
  description?: string;
  status?: string;
}

export interface AppUpdateBody {
  slug?: string;
  name?: string;
  description?: string;
  status?: string;
}
